#include "main.h"
#include <stdio.h>

/**
 * puts2 - prints every character of a string by new line
 * @str: the string containing character.
 *
*/

void puts2(char *str)
{
	int i = 0;

	while (str[i] != '\0')
	{
		putchar(s[i]);
		i++;
	}
	putchar('\n');
}
